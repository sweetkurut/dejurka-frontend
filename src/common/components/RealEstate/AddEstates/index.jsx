import styles from "./style.module.scss";

const AddEstates = () => {
  return (
    <div className={styles.wrapper}>
      <button className={styles.add_btn}>Добавить недвижимость</button>
    </div>
  );
};

export default AddEstates;
